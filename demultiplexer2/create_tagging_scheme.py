import glob
import find_file_pairs

# TODO
# collect all file pairs from input folder, give warning if unpaired files are found
# collect primers / tags / names from primer set
# request input for combinations (form?)
